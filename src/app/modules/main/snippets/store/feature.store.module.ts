import { NgModule } from "@angular/core";
import {
    StoreModule,
    createFeatureSelector,
    createSelector,
    ActionReducerMap,
    ActionReducer,
    MetaReducer
} from "@ngrx/store";

// export interface FeatureState {
//   }

// export const FeaturerRducers: ActionReducerMap<FeatureState> = {
//   };

@NgModule({
    imports: []
})
export class FeatureStoreModule {}
